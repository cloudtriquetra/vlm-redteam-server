torch>=2.5.0
torchvision
torchaudio
transformers>=4.40.0,<5.0.0
accelerate>=0.26.0          # required by pipeline() with float16
Pillow>=10.0.0
fastapi>=0.110.0
uvicorn>=0.29.0
pyyaml>=6.0
soundfile>=0.12.0           # audio decoding
librosa>=0.10.0             # audio resampling
numpy>=1.24.0               # required by librosa

# Optional — only needed for source: s3 in models.yaml
# boto3>=1.34.0
